# Specification: Convert_JSON_to_CSV

## Expected Functionality
This project implements Convert Json To Csv.

This script take a json file as input and generate a csv file in output.
* json
* Run `pip install json` to install required external modules.

## Input
- User inputs as required by the application
- Configuration parameters as needed

## Expected Output
- Results based on the application's functionality
- Status messages and feedback to the user

## Usage
```bash
python app.py
```
