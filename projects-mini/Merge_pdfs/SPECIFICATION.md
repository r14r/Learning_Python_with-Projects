# Specification: Merge_pdfs

## Expected Functionality
This project implements Merge Pdfs.

A simple python script which when executed merges two pdfs
Run - "pip install PyPDF2"
It can be executed by running "python merge_pdfs.py"

## Input
- User inputs as required by the application
- Configuration parameters as needed

## Expected Output
- Results based on the application's functionality
- Status messages and feedback to the user

## Usage
```bash
python app.py
```
